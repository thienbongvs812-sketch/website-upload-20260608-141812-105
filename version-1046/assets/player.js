function startMoviePlayer(url){var video=document.getElementById('movie-player');var cover=document.getElementById('play-cover');if(!video||!url)return;function play(){if(cover)cover.classList.add('hidden');if(video.canPlayType('application/vnd.apple.mpegurl')){if(video.src!==url)video.src=url;video.play()}else if(window.Hls&&window.Hls.isSupported()){if(!video._hlsReady){var hls=new Hls();hls.loadSource(url);hls.attachMedia(video);video._hlsReady=true}video.play()}else{video.src=url;video.play()}}if(cover)cover.addEventListener('click',play);video.addEventListener('click',function(){if(video.paused)play()});}